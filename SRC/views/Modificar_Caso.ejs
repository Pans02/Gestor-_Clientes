<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modificar Caso</title>
    <link href="/css/main.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <div class="container mt-5">
        <h2 class="text-center">Modificar Caso</h2>

        <form action="/modificar_caso" method="POST">
            <input type="hidden" name="id_consulta" value="<%= caso.id_consulta %>">
            
            <div class="form-group">
                <label for="rut_cliente">RUT Cliente</label>
                <input type="text" id="rut_cliente" name="rut_cliente" class="form-control" value="<%= caso.rut_cliente %>" disabled>
            </div>

            <div class="form-group">
                <label for="nombre_cliente">Nombre Cliente</label>
                <input type="text" id="nombre_cliente" name="nombre_cliente" class="form-control" value="<%= caso.nombre_cliente %>" required>
            </div>

            <div class="form-group">
                <label for="correo_cliente">Correo Cliente</label>
                <input type="email" id="correo_cliente" name="correo_cliente" class="form-control" value="<%= caso.correo_cliente %>" required>
            </div>

            <div class="form-group">
                <label for="estado">Estado</label>
                <select id="estado" name="estado" class="form-control">
                    <option value="1" <%= caso.estado === true || caso.estado === 1 ? 'selected' : '' %>>Terminado</option>
                    <option value="0" <%= caso.estado === false || caso.estado === 0 ? 'selected' : '' %>>En Proceso</option>
                </select>
            </div>
            

            <button type="submit" class="btn btn-primary mt-3">Guardar Cambios</button>
        </form>

        <a href="/Tabla_Gestor" class="btn btn-secondary mt-3">Volver a la tabla</a>
    </div>
    
</body>

</html>
