<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <title>Agregar Caso | Sentinel</title>

    <link href="/css/theme.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        .form-box {
            max-width: 600px;
            margin: auto;
            margin-top: 40px;
            background: white;
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.07);
        }
    </style>
</head>

<body>

    <!-- NAVBAR -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow">
        <div class="container-fluid">
            <a class="navbar-brand" href="/Tabla_Gestor">
            </a>

            <div class="ms-auto">
                <a href="/Perfil" class="nav-link text-white d-inline">Perfil</a>
                <a href="/logout" class="btn btn-danger btn-sm ms-3">Cerrar sesión</a>
            </div>
        </div>
    </nav>

    <!-- FORMULARIO -->
    <div class="form-box">

        <h3 class="mb-4 text-center" style="color: var(--primary);">
            <i class="fa fa-file-circle-plus"></i> Registrar Nuevo Caso
        </h3>

        <form action="/Agregar_Caso" method="POST">

            <div class="mb-3">
                <label class="form-label">RUT del Cliente</label>
                <input type="text" name="rut_cliente" maxlength="16" class="form-control" placeholder="Ej: 12.345.678-9" required>
            </div>

            <div class="mb-3">
                <label class="form-label">Nombre del Cliente</label>
                <input type="text" name="nombre_cliente" maxlength="50" class="form-control" required>
            </div>

            <div class="mb-3">
                <label class="form-label">Correo del Cliente</label>
                <input type="email" name="correo_cliente" maxlength="50" class="form-control" required>
            </div>

            <button class="btn btn-primary w-100">Guardar Caso</button>
            <a href="/Tabla_Gestor?section=tabla" class="btn btn-secondary w-100 mt-3">Cancelar</a>

        </form>
    </div>

</body>

</html>
